import {
  fetchRandomWorkout,
  fetchRandomWorkouts,
  clearStoragePath,
  startRecording,
  saveRecordedAudioBuffer,
} from 'application/randomWorkouts/infrastructure/api';

export default {
  fetchRandomWorkout,
  fetchRandomWorkouts,
  clearStoragePath,
  startRecording,
  saveRecordedAudioBuffer,
};
